@if (Units is UnitsType.Metric)
{
    <div class="t1">@Value.ToDecimal()<span class="unit">°C</span></div>
    <div class="t2">@Value.ToFahrenheit()<span class="unit">°F</span></div>
}
else
{
    <div class="t1">@Value.ToDecimal()<span class="unit">°F</span></div>
    <div class="t2">@Value.ToCelsius()<span class="unit">°C</span></div>
}

@code {
    [Parameter] public UnitsType Units { get; set; }
    [Parameter] public float Value { get; set; }
}
